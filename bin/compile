set -e

# debug
# set -x

# parse and derive params
indent() {
  sed -u 's/^/-----> /'
}


BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

ls -R $BUILD_DIR/.apt/

mkdir -p ./app/tdlib

mkdir -p ./app/tdlib/test
mkdir -p $BUILD_DIR/.tdlib


ls -l ./app/tdlib